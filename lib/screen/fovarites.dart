import 'package:flutter/material.dart';

class Fovarites extends StatelessWidget {
  const Fovarites({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('fovarites trips');
  }
}